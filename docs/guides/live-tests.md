# Live Tests

Live tests are opt-in. Set the environment variables from `.env.example`, then run:

```bash
make live-test
```

The live suite is read-only by default and skips cleanly if credentials are missing.

