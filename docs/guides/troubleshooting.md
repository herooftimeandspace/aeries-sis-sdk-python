# Troubleshooting

## Common Issues

- If contract sync fails, confirm the Aeries support URLs are still reachable.
- If docs fail to build, run `make docs` locally to see the first strict-mode error.
- If live tests skip, check `AERIES_TEST_BASE_URL` and `AERIES_TEST_CERT`.

